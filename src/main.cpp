#include <iostream>
#include <string>
#include <limits>

#include "functions.h"

#define STREAM std::numeric_limits<std::streamsize>::max()

int main() {
    std::string name, pwd;
    int num;

    return 0;
}
